# Portfolio Landing (Angular + GSAP)

## Quick Start

```bash
npm install
npm start
```

## Structure

- `src/app/sections/*` contains reusable landing sections.
- `src/app/shared/directives/gsap-reveal.directive.ts` provides the GSAP reveal animation.
- `src/styles.scss` hosts the global theme and layout utilities.
